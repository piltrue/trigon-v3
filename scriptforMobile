local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Main loop 
task.spawn(function()
    while true do
        local args = {[1] = 2000}
        ReplicatedStorage:WaitForChild("TrainingUpdateRemote"):FireServer(unpack(args))
        task.wait(0.01)
    end
end)
